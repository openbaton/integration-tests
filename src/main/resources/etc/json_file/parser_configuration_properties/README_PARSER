Parser configuration file

The class Parser looks for a configuration file with this sintax:

old_value = new_value
...

In the file json, passed to the method Parser.randomize(), all the old_value will be replace with new_value.
IMPORTANT: in the file json the new_value must have the following sintax:

"some_parameter" = "<::new_value::>"

If we want to put random values:

old_value = new_value***
...

In the file json, passed to the method Parser.randomize(), all the old_value will be replace
with new_value plus 3 random characters (e.g. new_valuezxd).